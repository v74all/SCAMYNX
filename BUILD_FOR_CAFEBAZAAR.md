# 🏪 راهنمای ساخت نسخه Café Bazaar

## برای انتشار در کافه بازار

این راهنما برای ساخت و انتشار SCAMYNX در Café Bazaar است.

---

## ✅ پیش‌نیازها

- [x] API Keys واقعی در `secrets.properties` ✓
- [ ] Signing Key برای امضای APK
- [ ] حساب توسعه‌دهنده Café Bazaar
- [ ] تصاویر و اسکرین‌شات‌های برنامه

---

## 📝 مرحله 1: کانفیگ API Keys

**✅ انجام شده!** فایل `secrets.properties` با API های شما ساخته شد.

```properties
VIRUSTOTAL_API_KEY=5907b80a... (کلید واقعی شما ✓)
GOOGLE_SAFE_BROWSING_API_KEY=AIzaSyBJ... (کلید واقعی شما ✓)
URLSCAN_API_KEY=0199f1a7... (کلید واقعی شما ✓)
```

⚠️ **مهم:** این فایل در `.gitignore` است و به GitHub push نمی‌شود!

---

## 🔑 مرحله 2: ساخت Signing Key

برای انتشار در Café Bazaar، نیاز به signing key دارید:

```bash
# ساخت keystore (فقط یک بار)
keytool -genkey -v \
  -keystore ~/scamynx-release.keystore \
  -alias scamynx \
  -keyalg RSA \
  -keysize 2048 \
  -validity 10000

# سوالات را پاسخ دهید:
# - رمز عبور keystore (به خاطر بسپارید!)
# - نام و نام خانوادگی: Aiden
# - سازمان: V7LTHRONYX
# - شهر: Tehran
# - کشور: IR
```

**⚠️ خیلی مهم:** 
- فایل keystore و رمزش را در جای امن نگه دارید
- هرگز keystore را به GitHub push نکنید
- اگر keystore را گم کنید، نمی‌توانید برنامه را آپدیت کنید!

---

## 🔧 مرحله 3: کانفیگ Signing در Gradle

فایل `keystore.properties` بسازید:

```bash
# ساخت فایل
nano keystore.properties
```

محتوا:
```properties
storeFile=/home/aiden/scamynx-release.keystore
storePassword=YOUR_KEYSTORE_PASSWORD
keyAlias=scamynx
keyPassword=YOUR_KEY_PASSWORD
```

این فایل هم در `.gitignore` است ✓

---

## 🏗️ مرحله 4: Build Release APK

### الف) Clean Build

```bash
cd /home/aiden/Desktop/SCAMYNX
./gradlew clean
```

### ب) Build Release

```bash
./gradlew :app:assembleRelease
```

### ج) محل APK

```
app/build/outputs/apk/release/app-release.apk
```

### د) بررسی سایز

```bash
ls -lh app/build/outputs/apk/release/app-release.apk
```

باید حدود 10-20 MB باشد.

---

## 🧪 مرحله 5: تست APK

قبل از آپلود به Café Bazaar، حتماً تست کنید:

```bash
# نصب روی گوشی
adb install app/build/outputs/apk/release/app-release.apk

# یا
adb install -r app/build/outputs/apk/release/app-release.apk
```

### چک‌لیست تست:

- [ ] برنامه باز می‌شود
- [ ] اسکن URL کار می‌کند
- [ ] اسکن File کار می‌کند  
- [ ] اسکن VPN Config کار می‌کند
- [ ] اسکن Instagram کار می‌کند
- [ ] تغییر زبان (انگلیسی/فارسی) کار می‌کند
- [ ] تم تاریک/روشن عوض می‌شود
- [ ] History نمایش داده می‌شود
- [ ] ProGuard مشکلی ایجاد نکرده

---

## 📱 مرحله 6: آماده‌سازی برای Café Bazaar

### الف) اطلاعات برنامه

```
نام برنامه: SCAMYNX
نام انگلیسی: SCAMYNX - Scam Detector
Package Name: com.v7lthronyx.scamynx
نسخه: 1.0.0-beta1
Version Code: 1
دسته‌بندی: ابزارها / امنیت
```

### ب) توضیحات فارسی

```markdown
🛡️ SCAMYNX - شناساگر حرفه‌ای کلاه‌برداری

برنامه قدرتمند شناسایی کلاه‌برداری‌های اینترنتی برای اندروید

✨ امکانات:

🔍 اسکن چندگانه:
• آدرس‌های اینترنت (فیشینگ و لینک‌های مخرب)
• فایل‌ها (APK، برنامه‌ها، اسکریپت‌ها)
• کانفیگ VPN (VMESS، VLESS، Trojan، Shadowsocks)
• پروفایل اینستاگرام (شناسایی صفحات کلاه‌بردار)

🧠 تحلیل پیشرفته:
• هوش مصنوعی TensorFlow Lite
• اتصال به 6+ سرویس اطلاعات تهدید جهانی
• بررسی امنیت شبکه (TLS/SSL)
• امتیازدهی هوشمند ریسک

🎨 رابط کاربری مدرن:
• طراحی Material Design 3
• پشتیبانی کامل از راست‌چین فارسی
• تم تاریک و روشن
• انیمیشن‌های روان

🔒 حریم خصوصی:
• پردازش روی دستگاه (بدون ارسال داده)
• بدون جمع‌آوری اطلاعات شخصی
• متن‌باز و قابل بررسی

معماری تمیز، کد باکیفیت، تست‌های جامع
ساخته شده با عشق در ایران 🇮🇷

توسعه‌دهنده: Aiden (V7LTHRONYX)
```

### ج) توضیحات انگلیسی

```markdown
🛡️ SCAMYNX - Advanced Scam Detection

Powerful scam detection platform for Android using AI and threat intelligence.

✨ Features:
• Multi-target scanning (URLs, Files, VPN Configs, Instagram)
• AI-powered analysis with TensorFlow Lite
• 6+ threat intelligence providers
• Network security analysis
• Bilingual (English/Persian)
• Material Design 3 UI
• Privacy-focused (no data collection)

Made with ❤️ in Iran 🇮🇷
Developer: Aiden (V7LTHRONYX)
```

### د) اسکرین‌شات‌ها

نیاز به 4-8 اسکرین‌شات:

1. صفحه اصلی (انتخاب نوع اسکن)
2. نتیجه اسکن URL
3. نتیجه اسکن File
4. صفحه History
5. صفحه Settings (تغییر زبان)
6. تم تاریک
7. اسکن Instagram
8. اسکن VPN Config

**راهنما:**
```bash
# گرفتن اسکرین‌شات
adb shell screencap -p /sdcard/screenshot.png
adb pull /sdcard/screenshot.png ~/Desktop/
```

### ه) آیکون

آیکون برنامه از `app/src/main/res/mipmap-*/ic_launcher.png`

---

## 🚀 مرحله 7: آپلود به Café Bazaar

### 1. ورود به پنل

https://devapi.cafebazaar.ir/

### 2. ساخت برنامه جدید

- عنوان: SCAMYNX
- Package Name: com.v7lthronyx.scamynx
- دسته: ابزارها

### 3. آپلود APK

- فایل: `app-release.apk`
- نسخه: 1.0.0-beta1

### 4. اطلاعات تکمیلی

- توضیحات فارسی/انگلیسی
- اسکرین‌شات‌ها
- آیکون
- ویدیو معرفی (اختیاری)

### 5. قیمت‌گذاری

- رایگان ✓

### 6. مجوزها

برنامه از این مجوزها استفاده می‌کند:
- `INTERNET` - برای API calls
- `ACCESS_NETWORK_STATE` - بررسی اتصال

### 7. ارسال برای بررسی

پس از تکمیل، روی "ارسال برای بررسی" کلیک کنید.

---

## ⏱️ زمان بررسی

معمولاً 1-3 روز کاری طول می‌کشد.

---

## 📊 بعد از انتشار

### نظارت

- تعداد دانلود
- نظرات کاربران
- گزارش خطاها
- امتیاز برنامه

### آپدیت‌های بعدی

برای آپدیت:
```bash
# 1. افزایش version در app/build.gradle.kts
versionCode = 2
versionName = "1.0.1"

# 2. Build جدید
./gradlew clean assembleRelease

# 3. آپلود APK جدید در پنل کافه‌بازار
```

---

## 🆘 رفع مشکلات رایج

### مشکل: ProGuard crash می‌کند

```bash
# بررسی mapping
cat app/build/outputs/mapping/release/mapping.txt
```

اگر کلاسی obfuscate شده، به `proguard-rules.pro` اضافه کنید:
```proguard
-keep class com.v7lthronyx.scamynx.** { *; }
```

### مشکل: API rate limit

در توضیحات برنامه ذکر کنید:
"به دلیل محدودیت API های رایگان، تعداد اسکن روزانه محدود است."

### مشکل: سایز APK بزرگ است

```bash
# Build با Bundle
./gradlew :app:bundleRelease

# فایل AAB در:
app/build/outputs/bundle/release/app-release.aab
```

---

## 🎯 نکات مهم

### ✅ انجام دهید:
- قبل از هر release، تست کامل
- Version code را افزایش دهید
- Changelog بنویسید
- به نظرات کاربران پاسخ دهید

### ❌ انجام ندهید:
- Keystore را گم نکنید
- API keys را در کد hardcode نکنید
- بدون تست release نکنید
- نظرات منفی را نادیده نگیرید

---

## 📞 پشتیبانی

در صورت مشکل:
1. Logs را بررسی کنید: `./gradlew :app:assembleRelease --stacktrace`
2. ProGuard rules را چک کنید
3. در پنل کافه‌بازار پیام بگذارید

---

## ✅ چک‌لیست نهایی

- [x] API Keys در `secrets.properties` ✓
- [ ] Signing keystore ساخته شد
- [ ] Release APK build شد
- [ ] APK روی گوشی تست شد
- [ ] اسکرین‌شات‌ها گرفته شد
- [ ] توضیحات فارسی/انگلیسی نوشته شد
- [ ] حساب Café Bazaar آماده است
- [ ] APK آپلود شد

---

**موفق باشید! 🚀**

**توسعه‌دهنده:** Aiden (V7LTHRONYX)  
**تاریخ:** 6 فوریه 2026
